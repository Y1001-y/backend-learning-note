# 多模块编程
在idea中 可以通过创建多个独立的子模块来实现多模块 这些子模块互相独立 不会对其他子模块产生影响
子模块的`maven`和`jdk`版本是继承的父模块（`backend-learning`）的
## 父模块`backend-learning`的创建
在创建父项目时，必须创建的`springboot`项目 并且勾选`web`和`Lomkbok`等必须的依赖 检查项目的`Maven`和`jdk`版本
`Maven`版本是`4.1.1`，`jdk`采用`jdk21` 这些配置都会被后续子项目继承并且写在`pox.xml`文件中
其中`Java`版本和`jdk`要对应 并且类型要勾选为`springboot`（idea默认的是`Gradle-Groovy` 需要注意）
这里打包需要选择更加轻便的`Jar`包 配置类型不再使用传统臃肿的`properties`而是改成`YAML`(这里我已经存在了本地的`backend-learning` 就使用大写来演示)
点击下一步 就可以了（需要勾选创建`github`仓库）
![创建父级项目](/public/images/001.png)

## 项目初始化
由于使用的是多模块开发 这边可以删除父模块的`src`目录 根据项目规范编写`README.md`文件（也就是项目的简介与说明)并且完成首次推送 推送时要注意规范（关于指令的规范会单独整理一个文件 就不在这里赘述） 这里commit应该是：
```
git commit -m "chore: 创建了后端工程化学习仓库"
```
tips:
在每一次暂存之前都要执行`git status`指令 保证文件正确（可能有误删或者不需要的更改 这边可能就需要回滚或者取消更改） 然后执行`git add .`之后再次执行`git status`指令 确保工作树干净 ==因为只有工作树干净的情况下才能够提交 不然无法正常提交到远程仓库==

# 多模块的创建
## 创建模块`01-quickstart`
在父项目下创建新的模块 这里需要选择的是`Java`项目
命名为`01-quickstart` ==这里需要保留`src`== 这个项目可以共享刚刚创建的父级`springboot`项目的依赖 
tips:
- 这里的构建系统依旧要选择Maven
- `JDK`也要使用21
- 父项直接选择刚刚创建的`springboot`项目即可
- ==不需要添加示例代码==
![创建子项目](/public/images/002.png)
接下来直接创建接口并且进行测试 编写`README.md` 并且进行第二次提交
指令如下
```
# 查看当前工作树状态
git status

#暂存所有更改
git add .

# 提交
git commit -m "feat: 添加了01-quickstart 模块"
```
后续模块都是类似的操作
## 创建模块`02-config`
和模块1是类似的操作方式 这里就不再赘述了 操作完之后采用同样的指令提交 只不过添加的是02
后续的03-14模块还是同样的操作 完成之后提交即可

