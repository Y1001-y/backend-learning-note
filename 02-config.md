在这个模块 将记录如何创建包、修改配置文件、启动主类、读取配置文件（单个or批量读取）包括官方配置与自定义配置内容 

# 读取单个配置文件
## 配置文件
在`resources`文件夹中创建配置文件`application.yml`
其中端口号为9999 不再使用本地默认的端口号（防止被占用引起的问题）
内容如下
```java
sever:
	prot: 9999
	
	spring:
		application:02-配置管理
		
mqxu:
	name: 杨逸
	job: 学生
```
Tips：`yml`格式都是`:`的形式 一个配置相关信息只需要在项目写进行 不需要重复写等式 比传统的`propertise`更加简洁。
## 编写代码
使用`@value`读取单个配置（一般三个以内才会使用，批量读取会在后续提到）
代码示例：
![读取单个配置示例](/public/images/003.png)
编写完成之后还是先提交`github`远程仓库

# 连接` apifox`
## 前置准备
###  下载安装`apifox`并且获取令牌
下载、安装`apifox`，打开之后可以创建团队。然后创建模块。这边先创建父模块 再在父模块下创建子模块 需要和`idea`中的模块一一对应。
![apifox创建模块](/public/images/004.png)
Tips：第一次需要配置公钥 配置完成之后就不可以再查看 需要在退出之前妥善保管 配置步骤如下：
1.打开`apifox`中的账号设置并且找到API访问令牌
![创建令牌](/public/images/006.png)![创建令牌](/public/images/007.png)


2.点击新建 根据自己的需求命名令牌并且选择有效期（这里只是测试就选择了无限期 正常使用时需要注意命名规范和期限）
![创建令牌](/public/images/008.png)
3.都填写好之后点击保存并且生成令牌即可 令牌可以直接复制 需要注意下方的提醒 妥善保管令牌
![创建令牌](/public/images/009.png)
## 连接idea
在`idea`中下载插件`ApifoxHelper`
![安装插件](/public/images/011.png)


完成下载之后打开下面的`Apifox Helper` 第一次需要绑定令牌 只需要将刚刚生成的令牌粘贴进去 不放心可以测试一下（下面有测试令牌按钮 点击一下 显示成功就是没问题了） 然后点击加号 将我们idea中的两个模块与刚刚在`apifox`中创建的两个模块一一对应 这样就绑定成功了
![连接idea与apifox](/public/images/013.png)

然后回到接口文件 右键会有一个`upload to Apifox`  点一下 再回到`apifox`中刷新一下 应该就可以看见对应的接口了

![将接口上传到apifox](/public/images/014.png)![将接口上传到apifox](/public/images/015.png)

## 配置`apifox`中的环境
这边配置开发环境作为示例 端口号需要填写配置文件中的端口号 特别注意这里要写`http`不能写`https` 不然会连接失败的

![配置环境](/public/images/017.png)

## 运行测试
完成配置之后回到`idae`中启动项目 看见项目成功启动之后 再回`apifox`中 点击发送信息 就会显示对应的内容 到这里就已经成功完成了`apifox`与`idea`的连接和测试啦~
特别提醒：这里的两个端口是互不干扰的 因为两个模块是互不影响的 可以同时在`idea`中启动01和02 在`apifox`中是都可以正常接收到的
![连接测试](/public/images/018.png)

